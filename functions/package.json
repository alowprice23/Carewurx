{
  "name": "carewurx-functions",
  "description": "Cloud Functions for Carewurx",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "build": "echo 'No build step required for JavaScript functions, unless using TypeScript or bundlers'"
  },
  "engines": {
    "node": "18"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^11.9.0",
    "firebase-functions": "^4.3.1"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-plugin-promise": "^6.0.0"
  },
  "private": true
}
