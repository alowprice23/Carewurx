service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for initial testing of database logic
    // THIS IS NOT SECURE FOR PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
